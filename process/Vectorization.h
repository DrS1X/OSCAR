#pragma once
#include <string>
#include <vector>
#include <array>
#include <float.h>

using namespace std;

void Vectorization(vector<string>& oriFileNames, vector<string>& spFileNames, string outFolderPath);